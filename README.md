# poc-spring-redis

Spring Boot 3.x - Redis integration.

Use docker/redis-compose.yml file to launch a redis cluster (7 nodes).

Redis: activate keyspace events

On Spring Boot side, listen to key expirations

User Bruno collection to insert/get/delete simple key/value.